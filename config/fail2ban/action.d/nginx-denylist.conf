[Definition]
actionstart =
actionstop =
actioncheck =
actionban = sh -c 'echo "deny <ip>;" >> /etc/nginx/denylist.conf && nginx -s reload'
actionunban = sh -lc 'tmp=$(mktemp); grep -vE "^[[:space:]]*deny[[:space:]]+<ip>;" /etc/nginx/denylist.conf > "$tmp" && cat "$tmp" > /etc/nginx/denylist.conf && rm -f "$tmp" && nginx -s reload'
