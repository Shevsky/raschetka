import { identityProxy } from '~/utils/misc/identity-proxy';

// Все состояния объявляются здесь ⬇️ чтобы их имена точно не пересекались и были уникальными
export const { checkGroupWf } = identityProxy();
