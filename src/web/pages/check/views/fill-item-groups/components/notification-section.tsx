import { NotificationCard } from '~/web/components/cards/notification-card';

export const NotificationSection = () => {
  return <NotificationCard withClose>📋 Отметь общие позиции (если такие были). Они будут поделены между всеми</NotificationCard>;
};
